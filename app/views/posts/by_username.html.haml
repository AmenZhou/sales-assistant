.row
  = render 'menu'
.row
  .col-md-offset-5
    %h3
      Username Search Page
.row
  .col-md-6
    = render 'search_links_head'
.row
  Usernames
  - User.pluck(:email).each do |email|
    = link_to email, polymorphic_path([:by_username, controller.controller_name], username: email), class: 'btn btn-default'

#posts_list
  = render partial: 'posts_list', :posts => @posts
= will_paginate @posts
