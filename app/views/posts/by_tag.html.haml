.row
  = render 'menu'
.row
  .col-md-offset-5
    %h3
      Tag Search Page
.row
  .col-md-6
    = render 'search_links_head'
.row
  Tags
  - tag_cloud Post.tag_counts, %w{s m l} do |tag, css_class|
    = link_to tag.name, polymorphic_path([:by_tag, controller.controller_name], tag: tag.name), class: 'btn btn-default'

#posts_list
  = render partial: 'posts_list', :posts => @posts
= will_paginate @posts
