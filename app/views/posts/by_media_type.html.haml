.row
  = render 'menu'
.row
  .col-md-offset-5
    %h3
      Media Type Search Page
.row
  .col-md-6
    = render 'search_links_head'
.row
  Media Types
  - Post::MediaType.each do |mt|
    = link_to mt, polymorphic_path([:by_media_type, controller.controller_name], media_type: mt), class: 'btn btn-default'

#posts_list
  = render partial: 'posts_list', :posts => @posts
= will_paginate @posts
