.row
  = render 'menu'
.row
  .col-md-offset-5
    %h3
      Category Search Page
.row
  .col-md-6
    = render 'search_links_head'
.row
  Categories
  - @categories.pluck(:name).each do |c|
    = link_to c, polymorphic_path([:by_category, controller.controller_name], category: c), class: 'btn btn-default'

#posts_list
  = render partial: 'posts_list', :posts => @posts
= will_paginate @posts
